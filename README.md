# Rest Project + TypeScript

This project is using TypeScript, Express y Rest.


## Instalation

1. Clone .env.template and rename it to .env
2. Execute `npm install` to install dependencies
3. Execute `docker-compose up -d` to create the database instance.
4. Execute `npm run dev` to run the project

